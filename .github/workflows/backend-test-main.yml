name: Backend Test Main

on:
  push:
    branches: [main]
    paths-ignore:
      - README.md
      - docs/**
      - '**/*.md'

permissions:
  contents: read

concurrency:
  group: backend-test-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: go test + vet
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Test
        uses: ./.github/actions/backend-test
